<Page x:Class="CynologicalCenter.UI.Pages.DogsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Собаки"
      Background="{StaticResource BackgroundBrush}"
      Loaded="Page_Loaded">

    <Grid>
        <Rectangle Fill="{StaticResource PawPatternBrush}" IsHitTestVisible="False"/>

        <Grid Margin="28">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
                <TextBlock Text="Собаки"
                           Style="{StaticResource PageTitle}"
                           Margin="0"/>
                <Border Background="#E8F0EE" CornerRadius="5"
                        Padding="12,4" Margin="12,0,0,0"
                        VerticalAlignment="Center">
                    <TextBlock x:Name="TxtCount"
                               Foreground="{StaticResource PrimaryBrush}"
                               FontWeight="Bold" FontSize="13"/>
                </Border>
            </StackPanel>

            <Border Grid.Row="1" Style="{StaticResource Card}"
                    Margin="0,0,0,16" Padding="16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0"
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1.5" CornerRadius="8"
                            Background="White" MaxWidth="360"
                            HorizontalAlignment="Left">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔍" FontSize="14"
                                       Margin="12,0,0,0"
                                       VerticalAlignment="Center"
                                       Foreground="{StaticResource TextLightBrush}"/>
                            <TextBox x:Name="TxtSearch"
                                     BorderThickness="0" Height="36"
                                     Padding="8,0" FontSize="13"
                                     Width="300" Background="Transparent"
                                     VerticalContentAlignment="Center"
                                     TextChanged="TxtSearch_TextChanged"/>
                        </StackPanel>
                    </Border>

                    <Button x:Name="BtnAdd"
                            Grid.Column="1"
                            Content="Додати собаку"
                            Style="{StaticResource PrimaryButton}"
                            Click="BtnAdd_Click"/>
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="2">
                <WrapPanel x:Name="WrapDogs" Orientation="Horizontal"/>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>