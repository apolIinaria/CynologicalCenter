<Page x:Class="CynologicalCenter.UI.Pages.CoursesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Курси"
      Loaded="Page_Loaded">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Курси"
                   FontSize="22" FontWeight="Bold"
                   Margin="0,0,0,16"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal"
                    Margin="0,0,0,12">
            <Button Content="+ Додати курс"
                    Height="32" Padding="16,0"
                    Background="#2563EB" Foreground="White"
                    BorderThickness="0"
                    Click="BtnAdd_Click"/>
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="GridCourses"
                  AutoGenerateColumns="False"
                  IsReadOnly="True" CanUserAddRows="False"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  SelectionMode="Single"
                  SelectionChanged="GridCourses_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID"
                    Binding="{Binding CourseId}" Width="50"/>
                <DataGridTextColumn Header="Назва"
                    Binding="{Binding CourseName}" Width="200"/>
                <DataGridTextColumn Header="Опис"
                    Binding="{Binding Description}" Width="250"/>
                <DataGridTextColumn Header="Ціна (грн)"
                    Binding="{Binding Price}" Width="100"/>
                <DataGridTextColumn Header="Мін. вік (міс)"
                    Binding="{Binding MinAgeMonths}" Width="110"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    Margin="0,12,0,0">
            <Button x:Name="BtnEdit"
                    Content="✏ Редагувати"
                    Height="32" Padding="16,0" Margin="0,0,8,0"
                    Background="#0F172A" Foreground="White"
                    BorderThickness="0" IsEnabled="False"
                    Click="BtnEdit_Click"/>
            <Button x:Name="BtnDelete"
                    Content="🗑 Видалити"
                    Height="32" Padding="16,0"
                    Background="#DC2626" Foreground="White"
                    BorderThickness="0" IsEnabled="False"
                    Click="BtnDelete_Click"/>
        </StackPanel>
    </Grid>
</Page>