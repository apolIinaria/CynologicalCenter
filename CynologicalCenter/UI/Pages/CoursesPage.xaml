<Page x:Class="CynologicalCenter.UI.Pages.CoursesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Курси"
      Background="{StaticResource BackgroundBrush}"
      Loaded="Page_Loaded">

    <Grid>
        <Rectangle Fill="{StaticResource PawPatternBrush}"
                   IsHitTestVisible="False"/>

        <Grid Margin="28">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Курси" Style="{StaticResource PageTitle}"/>

            <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,16" Padding="16">
                <Button x:Name="BtnAdd"
                        Content="Додати курс"
                        Style="{StaticResource PrimaryButton}"
                        HorizontalAlignment="Left"
                        Click="BtnAdd_Click"/>
            </Border>

            <ScrollViewer Grid.Row="2">
                <WrapPanel x:Name="WrapCourses" Orientation="Horizontal"/>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>