<Window x:Class="CynologicalCenter.UI.Dialogs.DogReportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Звіт по собаці" Height="680" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Звіт по собаці"
                   Style="{StaticResource PageTitle}"/>

        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="Основна інформація"
                               Style="{StaticResource SectionTitle}"/>
                    <Grid x:Name="GridInfoContent"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="Статистика навчання"
                               Style="{StaticResource SectionTitle}"/>
                    <Grid x:Name="GridStatsContent"/>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <StackPanel>
                <TextBlock Text="Хронологія занять"
                           Style="{StaticResource SectionTitle}"
                           Margin="20,20,20,8"/>
                <DataGrid x:Name="GridHistory"
                          Style="{StaticResource ModernDataGrid}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Дата"
                            Binding="{Binding [дата_час]}" Width="140"/>
                        <DataGridTextColumn Header="Курс"
                            Binding="{Binding [курс]}" Width="160"/>
                        <DataGridTextColumn Header="Тренер"
                            Binding="{Binding [тренер]}" Width="160"/>
                        <DataGridTextColumn Header="Статус"
                            Binding="{Binding [status]}" Width="110"/>
                        <DataGridTextColumn Header="Оцінка"
                            Binding="{Binding [grade]}" Width="80"/>
                        <DataGridTextColumn Header="Рейтинг"
                            Binding="{Binding [рейтинг]}" Width="100"/>
                        <DataGridTextColumn Header="Коментар"
                            Binding="{Binding [коментар]}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>
    </Grid>
</Window>