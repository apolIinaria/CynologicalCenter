<Window x:Class="CynologicalCenter.UI.Dialogs.VaccinationAlertDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Прострочена вакцинація" Height="500" Width="700"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal"
                    Margin="0,0,0,16">
            <TextBlock Text="Прострочена вакцинація"
                       Style="{StaticResource PageTitle}"
                       Margin="0"/>
            <Border x:Name="CountBadge"
                    Background="#FEF3C7"
                    CornerRadius="20" Padding="12,4"
                    Margin="12,0,0,0"
                    VerticalAlignment="Center">
                <TextBlock x:Name="TxtCount"
                           Foreground="{StaticResource WarningBrush}"
                           FontWeight="Bold" FontSize="13"/>
            </Border>
        </StackPanel>

        <Border Grid.Row="1"
                Background="#FEF3C7"
                CornerRadius="10" Padding="16"
                Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="⚠" FontSize="20"
                           Margin="0,0,12,0"
                           VerticalAlignment="Center"/>
                <TextBlock Text="Цим собакам необхідно оновити вакцинацію. Зверніться до власників якомога швидше."
                           FontSize="13"
                           Foreground="#92400E"
                           VerticalAlignment="Center"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource Card}"
                Padding="0">
            <DataGrid x:Name="GridExpired"
                      Style="{StaticResource ModernDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Кличка"
                        Binding="{Binding Nickname}" Width="120"/>
                    <DataGridTextColumn Header="Порода"
                        Binding="{Binding BreedName}" Width="150"/>
                    <DataGridTextColumn Header="Власник"
                        Binding="{Binding OwnerName}" Width="180"/>
                    <DataGridTextColumn Header="Телефон"
                        Binding="{Binding Phone}" Width="140"/>
                    <DataGridTextColumn Header="Остання вакцинація"
                        Binding="{Binding LastVaccination,
                            StringFormat=dd.MM.yyyy}" Width="150"/>
                    <DataGridTextColumn Header="Днів минуло"
                        Binding="{Binding DaysSinceVaccination}"
                        Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Window>