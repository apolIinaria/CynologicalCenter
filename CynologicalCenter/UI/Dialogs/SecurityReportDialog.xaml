<Window x:Class="CynologicalCenter.UI.Dialogs.SecurityReportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Інциденти безпеки" Height="680" Width="950"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal"
                    Margin="0,0,0,20">
            <TextBlock Text="Інциденти безпеки"
                       Style="{StaticResource PageTitle}"
                       Margin="0"/>
            <Border x:Name="BadgeTotal"
                    CornerRadius="20" Padding="12,4"
                    Margin="12,0,0,0" VerticalAlignment="Center">
                <TextBlock x:Name="TxtTotal"
                           FontWeight="Bold" FontSize="13"/>
            </Border>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource Card}"
                Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Зведений підсумок"
                           Style="{StaticResource SectionTitle}"/>
                <DataGrid x:Name="GridSummary"
                          Style="{StaticResource ModernDataGrid}"
                          MaxHeight="60"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource Card}"
                    Padding="0">
                <StackPanel>
                    <Border Background="#FEE2E2" CornerRadius="12,12,0,0"
                            Padding="16,12">
                        <TextBlock Text="Масові DELETE"
                                   FontWeight="SemiBold" FontSize="14"/>
                    </Border>
                    <DataGrid x:Name="GridDelete"
                              Style="{StaticResource ModernDataGrid}"
                              Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource Card}"
                    Padding="0">
                <StackPanel>
                    <Border Background="#FEF3C7" CornerRadius="12,12,0,0"
                            Padding="16,12">
                        <TextBlock Text="Помилки авторизації"
                                   FontWeight="SemiBold" FontSize="14"/>
                    </Border>
                    <DataGrid x:Name="GridErrors"
                              Style="{StaticResource ModernDataGrid}"
                              Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="4" Style="{StaticResource Card}"
                    Padding="0">
                <StackPanel>
                    <Border Background="#E8F0EE" CornerRadius="12,12,0,0"
                            Padding="16,12">
                        <TextBlock Text="Нічна активність"
                                   FontWeight="SemiBold" FontSize="14"/>
                    </Border>
                    <DataGrid x:Name="GridNight"
                              Style="{StaticResource ModernDataGrid}"
                              Margin="0,8,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>